# Changelog

## Completed
- follow-up discharged (completed) from spec-validator-repair-residuals: sbc: injectable CodecOptions.cache for per-tenant isolation (two codecs with dif… — evidence: 3a04be6
- [infer-nullable-not-mixed] 7990c42. Deviations: salvaged session-side from salvage/8f60bc49-u1 — the item's own gates were green and the unit salvage refused only because the narrowed cherry-pick targeted a commit an excluded sibling owned; merging the ref was conflict-free and the full suite went 45 files / 1928 passed with tsc clean; ladder: merge-infeasible, salvaged. · spec: spec-validator-repair-residuals
- abandoned spend: ~$77.99 equivalent API cost across 1 terminal non-COMPLETED item(s) (infer-nullable-not-mixed); recorded, not divided into per-item costs (run 8f60bc49)
- run-level spend: ~$3.26 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run 8f60bc49)
- [remove-legacy-config-form] 3e76b602725c8676879032477ed3755d92e7d41d. Deviations: deviated — Design point (b) raw-function-vs-no-config test uses an ASYNC raw arrow (async (value,errors)=>{}) not a sync one: only an async raw fn's emission differs pre/post-deletion, so this makes the assertion a real deletion detector (verified: 27 pass / 1 fail against branch-present HEAD source).; deviated — Stale anchors — printed :105/:107-109 located by symbol grep (isArrowFunction(configArg)); comment+branch were at :105/:107-109 in this tree as printed, removed exactly.; added — OBJECT_LITERAL_BASELINE frozen as a module-level const captured from the deterministic transform (uid.scope derives its namespace from the fixture file path, so transformCode output is byte-stable across runs); object-literal path is provably unaffected by the deleted branch, so post-deletion bytes equal pre-item bytes.; ladder: 1 attempt (critic), completed. Cost: ~$45.88 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair-residuals
- [sbc-compute-size] 88b52bd. Deviations: deviated — Design line anchors were stale: the `if (size !== -1)` guard was at :1925 (not :1888), the -1 sentinel assertions at :1945/:1949 (not :1908/:1912). Located by symbol/content per read discipline.; ladder: 1 attempt (critic), completed. Cost: ~$45.67 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair-residuals
- [unify-packed-numeric-tags] 0a459be. Deviations: none; ladder: 3 attempts (critic → fixer FIXED → critic), completed. Cost: ~$88.92 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair-residuals
- [bigint-int64-parity] b6f2d83. Deviations: none; ladder: 7 attempts (critic → replanner completed → implementer → critic → replanner completed → implementer → critic), completed. Cost: ~$87.50 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair-residuals
- [encode-growth-signal] 60c19542237e2bc3fec8ae34bb0831d1b85c9680. Deviations: added — Tagged-path >1MB case uses a top-level Float64Array (1.6MB) to exercise the packed-float64 growth loop directly, since a top-level typed array routes through the generic tryEncodeSbc path per src/sbc/index.ts:701.; ladder: 1 attempt (critic), completed. Cost: ~$36.04 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair-residuals
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run 8f60bc49)
- [readme-accuracy] b080c13. Deviations: salvaged session-side; its critic FAIL was collateral — the item's own gate npx tsc -p tsconfig.build.json --noEmit was red from unlanded siblings and is green (exit 0) on the salvaged tree; ladder: 2 critic FAILs, FABLE_REPLAN budget exhausted, salvaged. · spec: spec-validator-repair
- [sbc-cache-isolation] f6251d9. Deviations: salvaged session-side; the 'test evidence destroyed' guard blocked it on schema-store.test.ts net-removals that are in fact a RELOCATION to the mirror-named test/sbc/cache.test.ts — verified case-by-case; ladder: blocked on guard, salvaged. · spec: spec-validator-repair
- [sbc-encode-safety] 6a97663. Deviations: salvaged session-side from salvage/5776b305-u1; ladder: 2 attempts (critic, SEVERITY_FIXER), salvaged. · spec: spec-validator-repair
- [sbc-schema-preregistration] 126a52b. Deviations: salvaged session-side from salvage/5776b305-u1; ladder: 1 attempt, reverted, salvaged. · spec: spec-validator-repair
- [sbc-compile-time-parity] 83a53cf. Deviations: salvaged session-side from salvage/5776b305-u1; ladder: 2 attempts (critic, FABLE_REPLAN), reverted, salvaged. · spec: spec-validator-repair
- [encodable-type-constraint] e207b0f. Deviations: salvaged session-side from salvage/5776b305-u1 after the u1 batch merge went suite_red on unlanded siblings; ladder: 1 attempt, reverted, salvaged. · spec: spec-validator-repair
- [remove-map-set-tags] 9d23aea. Deviations: salvaged session-side from salvage/5776b305-u1; the engine's per-file 'test evidence destroyed' guard blocked it on the 17 Map/Set cases index.test.ts net-removes, which is the retirement itself; ladder: blocked on guard, salvaged. · spec: spec-validator-repair
- abandoned spend: ~$424.76 equivalent API cost across 12 terminal non-COMPLETED item(s) (encode-growth-signal, remove-map-set-tags, unify-packed-numeric-tags, encodable-type-constraint, infer-nullable-not-mixed, bigint-int64-parity, sbc-compile-time-parity, sbc-schema-preregistration, sbc-encode-safety, sbc-cache-isolation, sbc-compute-size, readme-accuracy); recorded, not divided into per-item costs (run 5776b305)
- run-level spend: ~$916.41 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run 5776b305)
- [codegen-uint16-hoist] 955b099492ab83ffb2bd59027276af8bccc2afe2. Deviations: none; ladder: 1 attempt (critic), completed. Cost: ~$21.31 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair
- [sbc-browser-platform-tests] c98199ac4412a5e1823f3ebeef7b398d284aaf2e. Deviations: deviated — Chose Design option (a): vitest.config.ts (a files-own path) left unmodified — the env pin is untouched because the browser impls are exposed for direct import instead of via a second no-Buffer vitest project.; deviated — The 'full encode→decode round-trip running entirely on browser bindings' is a hand-built tagged mini-codec over the browser primitives, not routed through codegen.ts — codegen is out of item scope and its codegenDriver is captured at compile time (not injectable), so a browser round-trip through the real codegen path is unreachable from this surface.; added — dvCache is now constructed unconditionally (was `if (!isNode)`), so getDv/browser DataView bindings are exercisable under the node test env; getDv stays browser-only on the hot path, so the Node fast path gains no per-call cost.; added — Exposed browser impls as an exported `browser` namespace object plus module-level named functions (browserAllocBuf/browserByteLen/browserCopyBuf/browserReadStr/browserReadF64/browserReadBI64/browserWriteF64/browserWriteBI64/browserWriteUtf8); anchors located by symbol grep, not printed line numbers.; ladder: 1 attempt (critic), completed. Cost: ~$23.61 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair
- [registry-hash-validation] a0643e3b94de311a3f17583963fd74c8b0fb4fc5. Deviations: deviated — schema.ts already exported computeShapeHash (line 296) and it is absent from both src/sbc/index.ts and src/index.ts export lists, so no schema.ts edit was needed — the Design's suggested export addition was already present.; ladder: 1 attempt (critic), completed. Cost: ~$14.89 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair
- [decoder-count-limits] e6c640c. Deviations: deviated — Design claimed six unguarded new Array(l) sites plus one guarded with the literal (7 total). Actual tree: all 14 generated allocation sites already carried the MAX_ARRAY_COUNT guard (prior batch) but as the magic literal 1048576 in the generated source; fix reduced to constant-izing all 14 via ${MAX_ARRAY_COUNT} interpolation. All printed line numbers were stale — anchors located by grepping the guard string, not by line.; deviated — Cross-path parity placed in test/sbc/codegen.test.ts, not test/sbc/index.test.ts as the Design's test plan suggested: index.test.ts is not in files-own, so editing it would be an out-of-bounds write that FAILs the item. tagged case-8 objects delegate to the compiled decodeFn, so the genuinely-tagged path is tag-7; the parity test asserts the tag-7 tagged decoder and the compiled object-field decoders (typed/string/bytes/object) both throw the identical 'Codec2: array count 2000000 exceeds limit' on the same declared count.; added — extract.ts pos-bounds checks after the fixed skips (elemSize / flag 1/2/3) are shadowed by existing downstream truncation checks (target fixed-size check, readVarint past-end), so the observable, non-vacuously-proven guard is the count cap; the pos checks are retained as Design-mandated defense-in-depth. Both committed extract tests assert the count cap (typed + generic), each proven non-vacuous by a worktree-local scratch-copy revert to HEAD.; ladder: 1 attempt (critic), completed. Cost: ~$38.06 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair
- [sbc-key-enumeration-parity] 930753a098863f5ea69cb2100dcee66121d2a79d. Deviations: deviated — Stale line anchors: matchSchema's count loop was at src/sbc/index.ts:192 inside the function at :185 (Design said :190-192); revalidateCached's at :347 inside the function at :342 (Design said :344-347). Located by symbol, not printed line.; added — Acceptance clause-1's specified discriminator ('asserted by the registry not growing') is vacuous: the fallback inferAndRegister returns the existing schema for a known shape WITHOUT re-registering, so the per-instance registry Map never grows with or without the fix, and encode output / store.set count / value-read count are all byte- and count-identical either way (the fix is a pure cache-hit/CPU optimization with no black-box behavioral signature). Added a filtered spy on Object.hasOwn (called ONLY by the two fixed count loops in all of src/sbc) as the non-vacuous discriminator; the worktree-local revert confirmed exactly this one test fails when the fix is reverted, the other 4 parity tests being correctness/consistency guards.; ladder: 1 attempt (critic), completed. Cost: ~$19.41 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair
- [validator-body-ast-rewrite] 97ffda2. Deviations: deviated — inline() parses markers by split(ERROR_SENTINEL) (odd entries = markers) rather than a regex; sentinels are String.fromCharCode(0/1/2) control chars — the Edit tool mangled literal \u escapes, and control chars are a stronger sentinel (cannot occur in user source). Functionally equivalent to the designed regex-marker map.; deviated — Non-static pushes reuse error.generate with a MESSAGE_PLACEHOLDER swapped for the raw argument expression (function replacer to avoid $ handling), because error.ts (resolvePath) is outside the writable surface and not exported — avoids duplicating path rendering per rule against restating.; deviated — Test-plan item 4 rewritten from `errors.push("saw errors.push('a')")` (which passes under the old regex too — vacuous) to a variable-string discriminator (`let note = "errors.push('ghost')"`), which the textual regex corrupts and the AST leaves intact — making the AST-vs-text assertion genuinely non-vacuous.; deviated — Audit-carried line anchors were near-accurate but located by symbol per read-discipline; deleted DISALLOWED_BODY_REGEX (was :21) and ERRORS_PUSH_REGEX (:23), rewrote parse and inline in place.; ladder: 1 attempt (critic), completed. Cost: ~$30.23 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run 5776b305)
- [validator-boolean-coercion] 042225b. Deviations: salvaged session-side from salvage/f177cf28-u1; the item's undeclared removes_tests was the run's root cause — two legacy edge-cases.test.ts assertions encoding the old String(source).toLowerCase() policy were updated to assert rejection; ladder: 1 attempt (critic), completed. · spec: spec-validator-repair
- [validator-proto-property-reads] 2ff27e3. Deviations: salvaged session-side from salvage/f177cf28-u1 after the u1 batch merge reverted on a sibling's suite_red; ladder: 1 attempt (critic), completed. · spec: spec-validator-repair
- [validator-recursive-types] 24af36d. Deviations: salvaged session-side from salvage/f177cf28-u1 after the u1 batch merge reverted on an unrelated sibling's suite_red; ladder: 2 attempts (critic FAIL high, FABLE_REPLAN), completed. · spec: spec-validator-repair
- abandoned spend: ~$238.50 equivalent API cost across 4 terminal non-COMPLETED item(s) (validator-recursive-types, validator-proto-property-reads, validator-boolean-coercion, remove-legacy-config-form); recorded, not divided into per-item costs (run f177cf28)
- run-level spend: ~$648.42 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run f177cf28)
- [residue-codec-false-positive] e25aa7a. Deviations: none; ladder: 1 attempt (critic), completed. Cost: ~$8.52 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair
- [remove-typed-array-codec] e4c96a45e22c25f4eb3c0b71f7126dc7318528a8. Deviations: none; ladder: 1 attempt (critic), completed. Cost: ~$2.40 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair
- [validator-container-fidelity] b4ba606. Deviations: deviated — Map/Set element loops use validateOrCopy (not raw generateTypeValidation as the Design showed) so an unknown key/value type copies through instead of populating the fresh container with undefined — matches the array pattern and preserves prior copy-through behavior.; added — All audit line numbers were stale; located each generator by symbol grep.; ladder: 1 attempt (critic), completed. Cost: ~$11.01 equivalent API cost (blended-rate approximation). · spec: spec-validator-repair
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run f177cf28)
- [error-path-fidelity] b2410b72e43b010c6cab3ae3ae820db09690333b. Deviations: follow-up completing C13 (custom messages resolved by schema position) which the original landing omitted; array/Set elements resolve via the ErrorMessages<U>[] first entry and tuples resolve positionally, while record values stay container-level because an index signature contributes no properties for extractMessages to flatten. Cost: UNMEASURED. · spec: spec-validator-repair
- [build-pipeline-e2e-tests] 7abf5d9e3baaccb5bee73e9e5e82c59866f4084a. Deviations: none for the landed scope; run 5e01222d's unit merges failed or the item never dispatched, so the work was recovered from salvage refs and completed session-side. error-path-fidelity's C13 (custom messages resolved by schema position to array elements and record values) is NOT implemented — the path model (C8/C17/C18) is. Cost: UNMEASURED. · spec: spec-validator-repair
- [error-path-fidelity] 29794768e68d0235193900c809b0827b6daf480b. Deviations: none for the landed scope; run 5e01222d's unit merges failed or the item never dispatched, so the work was recovered from salvage refs and completed session-side. error-path-fidelity's C13 (custom messages resolved by schema position to array elements and record values) is NOT implemented — the path model (C8/C17/C18) is. Cost: UNMEASURED. · spec: spec-validator-repair
- [numeric-constraint-validators] 7eb50907db735d18be68a979d6c7be1c7f12c86e. Deviations: none for the landed scope; run 5e01222d's unit merges failed or the item never dispatched, so the work was recovered from salvage refs and completed session-side. error-path-fidelity's C13 (custom messages resolved by schema position to array elements and record values) is NOT implemented — the path model (C8/C17/C18) is. Cost: UNMEASURED. · spec: spec-validator-repair
- [output-construction-safety] e170c52e104c569b320da7ab48bc7cbff0159344. Deviations: none for the landed scope; run 5e01222d's unit merges failed or the item never dispatched, so the work was recovered from salvage refs and completed session-side. error-path-fidelity's C13 (custom messages resolved by schema position to array elements and record values) is NOT implemented — the path model (C8/C17/C18) is. Cost: UNMEASURED. · spec: spec-validator-repair
- abandoned spend: ~$115.14 equivalent API cost across 3 terminal non-COMPLETED item(s) (output-construction-safety, error-path-fidelity, numeric-constraint-validators); recorded, not divided into per-item costs (run 5e01222d)
- run-level spend: ~$531.41 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run 5e01222d)
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run 5e01222d)
- [runtime-tojsonschema] 61171b73bc00d5b6d410773446ec033357b677ca. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [plugin-self-assertion] 06c4a76d19ecc4434f6987c56b633cf165c41a1b. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [compiler-annotation-extraction] db26e432f4e17a9c25ad788222ebd90e194c3834. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [annotated-validator-types] 424cc8b3b25effe03a3c624872a32a69583bfcd7. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [format-validators-correctness] cd8bb9f78f6c48144087ece2196109f2ea76f75b. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [format-validators-p0] 6402f95da79cea0e346c71955318f858cc4c5208. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [analyzer-schema-gaps] 88172429efda85b52675da8b8c121046ad9c14c5. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [analyzer-structural-types] 9a3be45d774e4b471e1073c57dcf332619fd9736. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [emitted-code-escaping] 064e846a5354538030aa077ddb1b2e52d2e76b4d. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [compiler-import-detection] e6440de9100f856f31343860f18c2e6375634baa. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [repair-brand-registration] 8526477d8841f764e37c4198005aed88b20322b1. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [repair-validator-config-compilation] 3668de2ad9b887835fa048a5cbd69f473367d436. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- [relocate-tests-and-benches] a22b306b46697b3b457eaa71016d2fdf93b1638e. Deviations: none; salvaged from run aa871fa6 — the unit merge failed on a test-JOIN conflict and the code was recovered by fast-forward from salvage/aa871fa6-u1, verified green. Cost: UNMEASURED (run journal lost to the failed merge). · spec: spec-validator-repair
- abandoned spend: ~$562.80 equivalent API cost across 17 terminal non-COMPLETED item(s) (relocate-tests-and-benches, repair-validator-config-compilation, output-construction-safety, analyzer-structural-types, analyzer-schema-gaps, format-validators-correctness, numeric-constraint-validators, annotated-validator-types, compiler-annotation-extraction, runtime-tojsonschema, compiler-import-detection, emitted-code-escaping, format-validators-p0, repair-brand-registration, error-path-fidelity, plugin-self-assertion, build-pipeline-e2e-tests); recorded, not divided into per-item costs (run aa871fa6)
- run-level spend: ~$1.83 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run aa871fa6)
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run aa871fa6)
- abandoned spend: ~$0.00 equivalent API cost across 0 terminal non-COMPLETED item(s); recorded, not divided into per-item costs (run ab5159b1)
- run-level spend: ~$0.95 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run ab5159b1)
- [json-schema-transform-wiring] d2e9aab0767163b3278a426b599ca38b6688318f. Deviations: deviated — Edited files-shared src/compiler/index.ts (not just files-own tests/json-schema.ts): the item Design names the toJsonSchema wiring changes in that file and HEAD did not yet contain them, so the read-vs-change carve-out applies — implemented DetectedCall.method, visit() dual-method matching, patterns '.toJsonSchema', and the eager-computation prepend/dedup branch there.; deviated — Design step 2 'a second type argument on a toJsonSchema call is ignored': errorMessagesType is still captured on DetectedCall unconditionally (build path unchanged) but is never read in the toJsonSchema branch, so it is ignored as specified.; ladder: 1 attempt (critic), completed. Cost: ~$35.31 equivalent API cost (blended-rate approximation). · spec: spec-validator-json-schema
- [config-constraint-extractor] bb2f8d45bf52da282a9f5f85f0c2fddd7112f277. Deviations: deviated — startsWith/endsWith emit anchor-only patterns ('^a\.b', '\.com$') vs zod's redundant '^a\.b.*'/'.*\.com$'; Draft 2020-12 patterns are unanchored substrings so the trailing/leading '.*' is redundant.; deviated — email/uuid emit format-only fragments; zod additionally emits a regex pattern. Per Design principle, format is annotation-only in 2020-12 and this package's runtime regex differs from zod's, so a pattern would over-claim/mismatch — deliberately unmapped.; added — Throwaway zod@4.4.3 parity dir created at ~/.claude/storage/zod-parity-check could not be removed — `rm` on ~/.claude paths is sandbox-denied from this target-repo session. It sits in the sanctioned storage scratch area (not the target repo checkout, so not out_of_tree_damage); safe to prune manually.; ladder: 1 attempt (critic), completed. Cost: ~$29.20 equivalent API cost (blended-rate approximation). · spec: spec-validator-json-schema
- [json-schema-emitter] 1255c0793fffcd5e02242b80816ee6554fc22530. Deviations: deviated — circular-reference object (properties undefined) emits bare {type:'object'} with NO additionalProperties:false, per the Design's stated recipe/notes ('recursive types emit bare { type: object }'), rather than the full object row's additionalProperties:false — the two object variants split on whether properties is defined; added — branded-integer/float tests define Brand<T,B> inline in the source string (repo convention from tests/branded-strings.ts) since src/types.ts brand aliases are not importable inside the createProgram virtual test.ts; ladder: 1 attempt (critic), completed. Cost: ~$17.22 equivalent API cost (blended-rate approximation). · spec: spec-validator-json-schema
- [validators-subpath-export] 29e9b54. Deviations: none; ladder: 1 attempt (critic), completed. Cost: ~$2.26 equivalent API cost (blended-rate approximation). · spec: spec-validator-json-schema
- [analyzer-root-entry] 5e59ffd5d92d461fcf3357b88c1602ff04017ce0. Deviations: deviated — rootCache declared as part of a comma-joined let statement with the existing cache (let cache = ..., rootCache = ...;) instead of a separate 'let rootCache = ...;' statement, per this repo's coding-standard for grouping module-level let declarations — behavior and WeakMap separation are unchanged from the Design.; ladder: 1 attempt (critic), completed. Cost: ~$9.03 equivalent API cost (blended-rate approximation). · spec: spec-validator-json-schema
- [json-schema-public-surface] 65f5b881fb284b843519d764cce8e9cd4ac5f6cb. Deviations: none; ladder: 1 attempt (critic), completed. Cost: ~$4.41 equivalent API cost (blended-rate approximation). · spec: spec-validator-json-schema
- [agent-test-script] a3157052e1f165a1e6ab7f2cc507c4e00f082ebb. Deviations: deviated — Placed agent:test before bench (alphabetical order per coding-standards.md member alphabetization) rather than strictly 'beside bench and test' order; still within the same bottom script group after the '--' separator key as specified.. Cost: ~$0.86 equivalent API cost (blended-rate approximation). · spec: spec-validator-json-schema
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run ab5159b1)
