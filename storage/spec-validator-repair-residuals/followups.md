# Follow-ups
- ai-orchestrator migration (predecessor audit section J) — carried from spec-validator-repair: author a separate follow-up spec against the D:\ai-orchestrator repo AFTER this spec's items land; it must confront the `.parse()`-throws vs `{ok, data, errors}` return-shape change (user-acknowledged), the zod-4.4.3 zero-coercion divergence (this package coerces `'30'` → 30, flipping a plan node with `{"age":"30"}` from rejected to accepted), the `S extends z.ZodType` generic inversion in actions/types.ts, the `@esportsplus/typescript` ^0.27.3 vs ^0.29.6 skew (typescript 5→7 — that repo's compiler plugins migrate first), and this spec's wire-format (tag 12 unified, 13/14 retired) plus schema-hash (int64 vocabulary, nullable-in-hash) changes.
- sbc: injectable CodecOptions.cache for per-tenant isolation (two codecs with different PersistentStores share shape definitions through the module singleton) — public-API add, Ask First; carried from spec-validator-repair (deferred out of sbc-cache-isolation). The predecessor's other follow-up (README:259 computeSize prose) is NOT carried — sbc-compute-size owns the README "Computing Encoded Size" rewrite directly.
