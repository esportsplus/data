# Follow-ups
- sbc: injectable CodecOptions.cache for per-tenant isolation (two codecs with different PersistentStores share shape definitions through the module singleton) — public-API add, Ask First; deferred out of sbc-cache-isolation
- [ ] README:259 computeSize prose re-documents exact-size (no sentinel) behavior when sbc-compute-size lands
